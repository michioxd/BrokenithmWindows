<?xml version="1.0" encoding="utf-8"?>
<Window
  x:Class="BrokenithmWindows.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:BrokenithmWindows"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  Title="Brokenithm"
>
  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <Grid Background="Black">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid x:Name="AppTitleBar" Height="40" Background="Transparent">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <Button
        x:Name="AppBackButton"
        Click="AppBackButton_Click"
        Background="Transparent"
        BorderThickness="0"
        Padding="8"
        VerticalAlignment="Center"
        ToolTipService.ToolTip="Back"
        Margin="4,0,0,0"
        IsEnabled="False"
      >
        <FontIcon Glyph="&#xE72B;" FontSize="14" Foreground="#A0A0A0" />
      </Button>

      <StackPanel
        Grid.Column="1"
        Orientation="Horizontal"
        VerticalAlignment="Center"
        Margin="12,0,0,0"
        Spacing="6"
      >
        <TextBlock Text="Brokenithm" FontSize="12" Foreground="#A0A0A0" />
        <TextBlock Text="-" FontSize="12" Foreground="#808080" />
        <TextBlock Text="{Binding Status}" FontSize="12" Foreground="#A0A0A0" />
      </StackPanel>

      <StackPanel
        Grid.Column="1"
        x:Name="RightTitleBarStack"
        Orientation="Horizontal"
        HorizontalAlignment="Right"
        VerticalAlignment="Center"
      >
        <Button
          x:Name="ControlsButton"
          Click="ControlsButton_Click"
          Background="Transparent"
          BorderThickness="0"
          Padding="8"
          ToolTipService.ToolTip="Show or hide controls"
        >
          <StackPanel Orientation="Horizontal" Spacing="6">
            <FontIcon Glyph="&#xE700;" FontSize="14" Foreground="#A0A0A0" />
            <TextBlock
              Text="Controls"
              FontSize="12"
              Foreground="#A0A0A0"
              VerticalAlignment="Center"
            />
          </StackPanel>
        </Button>
        <Button
          x:Name="FullScreenButton"
          Click="FullScreenButton_Click"
          Background="Transparent"
          BorderThickness="0"
          Padding="8"
          ToolTipService.ToolTip="Full screen"
        >
          <FontIcon x:Name="FullScreenIcon" Glyph="&#xE740;" FontSize="14" Foreground="#A0A0A0" />
        </Button>
      </StackPanel>
    </Grid>

    <ContentPresenter x:Name="MainContent" Grid.Row="1" />
  </Grid>
</Window>
